<div class="container">
  <h3><a href='/suggestions/new'>New Suggestion</a></h3>
<!-- Logic here is not quite working!!!  -->
<!-- This is mixed up with show.erb in users -->
  <% @suggestions.each do |s| %>
    <%if own_suggestion?(s) %>
  <ul>
    <li><%= s.title %> <a href="/suggestions/<%= s.id %>"> details</a></li>
  </ul>

  <% else %>
  <ul>
    <li><%= s.title %> - <%= s.description %> </li>
  </ul>
    <% end %>
  <% end %>
</div>

